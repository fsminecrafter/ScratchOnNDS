# Example Project

Place your example Scratch project here as `example.sb3`.

This is used as the fallback project when no other `.sb3` is found
in `fat:/scratch/` on the SD card.

On the SD card, this file should be at:
  fat:/scratch/example/example.sb3
